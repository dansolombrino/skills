interface:
  display_name: "Environment Sync"
  short_description: "Provision and verify matching rig environments"
  default_prompt: "Use $environment-sync to provision and verify the exact uv environment across approved Research 2.0 rigs."
