interface:
  display_name: "Rig Sync"
  short_description: "Declare rig paths, deploy revisions, sync artifacts"
  default_prompt: "Use $rig-sync to validate declared rig paths, deploy an authorized Research 2.0 revision, or synchronize selected artifacts, paired with $environment-sync."
