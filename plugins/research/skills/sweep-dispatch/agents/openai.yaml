interface:
  display_name: "Sweep Dispatch"
  short_description: "Dispatch waves with live status and ETA updates"
  default_prompt: "Use $sweep-dispatch to verify, dispatch, and monitor an approved GPU wave with timestamped ten-minute status and ETA updates through completion."
