interface:
  display_name: "Sweep Dispatch"
  short_description: "Dispatch and monitor GPU experiment waves safely"
  default_prompt: "Use $sweep-dispatch to smoke-test, commit, deploy, verify, dispatch, monitor, and recover an approved experiment wave across available GPU rigs."
