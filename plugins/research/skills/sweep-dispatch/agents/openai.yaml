interface:
  display_name: "Sweep Dispatch"
  short_description: "Dispatch and monitor GPU experiment waves safely"
  default_prompt: "Use $sweep-dispatch to environment-check, smoke-test, deploy, verify, dispatch, monitor, and recover an approved wave across GPU rigs."
