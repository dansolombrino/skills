interface:
  display_name: "Flywheel Reproduce"
  short_description: "Validate claim-bearing work in Flywheel"
  default_prompt: "Use $flywheel-reproduce to structure this claim-bearing source, run budgeted validation, and pause for my approval before authoring any plot."

dependencies:
  tools:
    - type: "mcp"
      value: "flywheel"
      description: "Flywheel MCP server"
      transport: "streamable_http"
      url: "https://flywheel.paradigma.inc/mcp-server"
