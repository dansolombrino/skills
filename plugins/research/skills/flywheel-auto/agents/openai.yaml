interface:
  display_name: "Flywheel Auto"
  short_description: "Run budgeted autonomous Flywheel research"
  default_prompt: "Use $flywheel-auto to advance this frontier under budget while pausing for my approval before authoring any plot."

dependencies:
  tools:
    - type: "mcp"
      value: "flywheel"
      description: "Flywheel MCP server"
      transport: "streamable_http"
      url: "https://flywheel.paradigma.inc/mcp-server"
