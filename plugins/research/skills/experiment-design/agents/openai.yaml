interface:
  display_name: "Experiment Design"
  short_description: "Design reproducible experiments and run identities"
  default_prompt: "Use $experiment-design to design a new experiment and agree on its run identity, checkpoints, and structured progress telemetry."
